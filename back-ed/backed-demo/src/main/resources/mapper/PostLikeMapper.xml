<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.back.backeddemo.mapper.PostLikeMapper">

    <insert id="insert">
        INSERT INTO post_like (user_id, post_id) VALUES (#{userId}, #{postId})
    </insert>

    <delete id="delete">
        DELETE FROM post_like WHERE user_id = #{userId} AND post_id = #{postId}
    </delete>

    <delete id="deleteByUser">
        DELETE FROM post_like WHERE user_id = #{userId}
    </delete>

    <select id="countByPost" resultType="int">
        SELECT COUNT(*) FROM post_like WHERE post_id = #{postId}
    </select>

    <select id="exists" resultType="int">
        SELECT COUNT(*) FROM post_like WHERE user_id = #{userId} AND post_id = #{postId}
    </select>

    <select id="listLikedPostIds" resultType="long">
        SELECT post_id FROM post_like WHERE user_id = #{userId}
    </select>

</mapper>

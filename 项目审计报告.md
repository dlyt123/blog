# 个人博客网站 · 全面审计报告

审计时间：2026-09-14
审计范围：前端（Vue 3）、后端（Spring Boot 4）、数据库（MySQL `blog` 库）
审计方式：**静态交叉比对 + 启动服务真调接口 + 查真库核对**（不只看代码）

---

## 一、结论摘要

| 审计项 | 结论 |
|--------|------|
| 功能是否闭环 | ✅ **完整连通**。前端调用的 100 个接口在后端 100% 存在；19 张表全部被代码读写 |
| 死代码 | ⚠️ **有少量，但都无害**（详见第三节），无「前后端断链」的死代码 |
| BUG | ❌ **发现 4 类真实缺陷，已全部修复并复测通过**（详见第四节） |
| 安全隐患 | ✅ **无高危问题**。7 项检查逐项通过，另有 1 处 REST 语义瑕疵（详见第五节） |

一句话：**功能是通的、数据是真的、没有安全漏洞；但接口的异常处理有明显的健壮性缺陷，已修好。**

---

## 二、功能闭环验证

### 2.1 接口连通性（前端 ↔ 后端）

- 后端共注册 **107 个接口**；前端 `api/index.js` 导出 **100 个调用函数**
- **前端调用的接口，100% 能在后端找到** ✅ 没有任何"调了一个不存在的接口"
- 后端多出 6 个接口，逐个核实：

| 接口 | 判定 |
|------|------|
| `GET /api/rss` | ✅ 给 RSS 阅读器用，本就无需前端调用 |
| `GET /sitemap.xml` | ✅ 给搜索引擎用 |
| `GET /api/health` | ✅ 给运维/监控探活用 |
| `POST /api/auth/logout` | ✅ 无状态 JWT，前端删 token 即可 |
| `GET /api/admin/export` | ✅ 前端用原生 `fetch` 调用（不走 axios 封装），是我第一轮正则漏判，**实际有在用** |
| `GET /api/series/{id}` | ⚠️ **确实没有任何前端调用**（前端只用列表 + 系列下文章两个接口）。属于多余的接口，留着不影响，也可以删 |

### 2.2 数据是否真落库（实测往返）

不是看代码，是真调接口看状态变化：

| 验证 | 结果 |
|------|------|
| 搜索 `keyword=博客` | 命中 4 篇（数据来自数据库）✅ |
| 搜索 `keyword=Vue` | 命中 4 篇 ✅ |
| 搜索不存在的词 | 命中 0 篇（不报错）✅ |
| 点赞 → 查状态 → 取消 | `False/0` → `True/1` → `False/0`，状态真的变化并持久化 ✅ |
| 收藏 → 查状态 → 取消 | `0/False` → `1/True` → `0/False` ✅ |

### 2.3 页面可用性

39 个页面（16 个公开页 + 23 个登录态/后台页）用手机尺寸实测渲染：**全部正常渲染、无横向溢出、无 JS 错误、无白屏**。

---

## 三、死代码清单

### 3.1 前端

| 内容 | 判定 |
|------|------|
| `api/index.js` 的 `markChatRead()` | ⚠️ 定义了但没人调用。对应的后端接口是存在的，`Messages.vue` 改用「打开会话即已读」的方式实现。**无害**，可留作接口文档 |
| 组件 / 路由 / 工具 | ✅ **0 个死组件**，所有 `.vue` 都被引用或路由懒加载 |

### 3.2 后端

| 内容 | 判定 |
|------|------|
| `PostFavoriteMapper.countByUser()` | ⚠️ 未被调用（实际用的是 `countActiveByUser`，语义更准）**无害** |
| `ReportMapper.countPending()` | ⚠️ 未被调用。它本可以给「内容举报」菜单加一个**待处理数量角标**，现在没做 → 属于**未用上的能力**，非缺陷 |
| `UserFollowMapper.listFollowingIds()` | ⚠️ 未被调用。**无害** |
| `GlobalExceptionHandler` / `HttpStatusAdvice` / `DataCleanupConfig` / `DataInitializer` / `ScheduledPublishConfig` | ✅ **自动化工具的误报**：这些是 Spring 通过 `@RestControllerAdvice` / `@Configuration` / `@Component` **自动加载**的，不需要被别的类显式引用 |

### 3.3 数据库

**19 张表全部被代码读写** ✅（引用次数 4~45 次不等），**没有死表、没有孤立字段**。

---

## 四、发现并修复的 BUG（4 类）

> 全部**已修复**，并用「90 个接口冒烟测试」复测：**异常从 13 个降到 1 个**（剩下那个是导出接口返回二进制文件、测试脚本解不了码，不是缺陷）。

### BUG 1：友链检测**必定超时**（影响管理功能）

- **现象**：后台点「检查友链」，浏览器报请求超时，但服务端其实还在跑
- **根因**：`LinkController.check()` **串行**逐个访问友链，每条超时上限 6 秒。站内有 **6 条**友链 → 最坏 36 秒，而前端 axios 只等 **15 秒** → 必然超时
- **修复**：改为**并发**检查（`parallelStream`），总耗时 ≈ 最慢的那一条
- **复测**：6 条友链 **7.9 秒**全部检查完成，状态全部正确 ✅

### BUG 2：缺少参数 / 参数格式错误 → 返回 500（应为 400）

- **现象**：`GET /api/search` 不带 `keyword` → 500「服务器开小差了」
- **根因**：`GlobalExceptionHandler` 只处理了业务异常和 404/405，**Spring 的 `MissingServletRequestParameterException` 等直接掉进兜底分支变成 500**
- **额外危害**：这类错误还会触发**异常告警邮件**，给管理员刷无意义的邮件
- **修复**：新增 4 个异常处理器，统一转成 400 + 明确提示
- **复测**：
  - 搜索缺参数 → `400 缺少必要参数：keyword` ✅
  - `id` 传成 `abc` → `400 参数格式不正确：id` ✅
  - 请求体不是合法 JSON → `400 请求内容格式不正确，请检查提交的数据` ✅

### BUG 3：动态 `SET` 拼成空语句 → SQL 语法错误 → 500

- **现象**：提交一个所有字段都为空的更新 → 500
- **根因**：6 个 Mapper 用了 `<set>` + 一堆 `<if>` 的动态更新。字段**全为 null 时 SET 子句为空**，SQL 变成 `UPDATE category WHERE id=?` → 数据库报语法错误
- **修复**：
  1. 在服务层加**必填校验**（分类/标签/友链/系列/文章/用户资料），从源头杜绝空 SET
  2. 新增 `common/Validate` 工具类统一处理
- **复测**：

| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 新建分类（空） | 500 | `400 分类名称不能为空` |
| 修改分类（空） | 500 | `400 分类名称不能为空` |
| 新建标签（空） | 500 | `400 标签名称不能为空` |
| 新建友链（空） | 500 | `400 友链名称不能为空` |
| 新建系列（空） | 500 | `400 系列名称不能为空` |
| 新建文章（空） | 500 | `400 文章标题不能为空` |
| 修改个人资料（空） | 500 | `400 没有需要修改的内容` |

### BUG 4：空数据直插数据库，靠 NOT NULL 约束报错

- **现象**：日志里全是 `Column 'name' cannot be null`，用户看到 500
- **根因**：Controller 把请求体直接交给 Mapper，没有做字段校验
- **修复**：同 BUG 3 的校验覆盖（新建 + 更新两条路径）

---

## 五、安全隐患排查（逐项实测）

| # | 检查项 | 结论 |
|---|--------|------|
| 1 | **SQL 注入** | ✅ Mapper XML 中 **0 处 `${}` 拼接**，全部使用 `#{}` 参数化；也没有动态 ORDER BY 拼接 |
| 2 | **XSS（存储型）** | ✅ 全站 4 处 `v-html`（文章详情 / 关于我 / 编辑器预览 / 写文章预览）**全部经过 `renderMarkdown`**，而它内部用 **DOMPurify 净化**（会剥掉 `<script>`、`onerror` 等事件属性） |
| 3 | **越权（IDOR）· 文章** | ✅ `PostService.checkOwner()` 校验：非管理员只能改/删自己的文章 |
| 4 | **越权 · 评论** | ✅ 评论删除只有 `/api/admin/comments/{id}` 一个入口，被 `AdminOnlyInterceptor` 限定为管理员。（普通用户没有删评论接口，所以不存在越权，但也意味着**用户不能删自己的评论**——功能取舍，非缺陷） |
| 5 | **越权 · 私信** | ✅ 会话与聊天记录都按 `from_user_id / to_user_id = 当前用户` 过滤，且全部接口强制登录 |
| 6 | **密码哈希泄露** | ✅ `GET /api/users/{id}` 是**显式白名单字段**（id/username/nickname/avatar/role/createTime），**不返回邮箱和密码**；`AuthService.safe()` 在返回前把 `password` 置空；`updateProfile` 强制把 `role` / `username` 置 null，**防止通过资料接口把自己提权成管理员** |
| 7 | **CORS 配置** | ✅ 默认只放行 `http://localhost:5173,http://127.0.0.1:5173`，**没有用 `*`**；且代码里明确注释了「不要用 `*` 配 allowCredentials(true)」 |
| 8 | **文件上传** | ✅ 类型白名单 + 大小限制（5MB）+ 路径 `normalize()` 校验，防止路径穿越 |
| 9 | **暴力破解 / 刷接口** | ✅ 三道独立防线：接口限流（按 IP）+ 图形验证码（一次性）+ 登录失败锁定（按**账号**，换 IP 也锁得住） |
| 10 | **敏感信息外泄** | ✅ 未处理异常只返回「服务器开小差了」，SQL 报错、类名、文件路径**只写服务端日志**；`application-local.yml` 已在 `.gitignore`，密码/授权码不进版本库 |
| 11 | **管理员接口授权** | ✅ `/api/admin/**` 全部由 `AdminOnlyInterceptor` 覆盖（唯一例外 `/api/admin/posts/**` 是**有意**放行的，因为博主也要管自己的文章，由 Controller 内部做细粒度校验） |

### ⚠️ 唯一的瑕疵（低危，未改）

**`GET /api/messages/with/{id}` 会修改数据**（顺带把消息标记为已读）。

- 严格来说违反 REST 语义：GET 应该是只读的
- 实际影响很小（不会造成错误），但如果将来加浏览器预取 / CDN 缓存，可能出现「消息被莫名其妙标记成已读」
- 若要改：由前端在打开会话后显式调一次 `PUT /messages/with/{id}/read`（**这个接口本来就有，前端的 `markChatRead()` 也已经封装好了**）

---

## 六、遗留建议（上线前）

1. **清理测试期日志**：`visit_log`（151 条）和 `operation_log`（64 条）都是本地反复测试产生的，上线前建议一并清掉，免得数据统计里混着测试数据
2. **`GET /api/series/{id}`** 这个没人用的接口可以删，也可以留着当扩展点
3. **`ReportMapper.countPending()`** 可以启用起来，给后台「内容举报」菜单加待处理角标
4. **消息已读**的 REST 语义瑕疵（见上方第五节末尾）

---

## 附：本次审计的验证手段

- 接口连通性：脚本解析 107 个后端路由 + 100 个前端调用做交叉比对
- 死代码：逐个检查前端导出函数、后端 Mapper 方法、类引用的被调用情况；数据库表检查代码引用次数
- 真实可用性：**启动服务，把 90 个接口逐个真调一遍**（写操作用不存在的 id + 空体探测，确保不污染数据）
- 数据落库：点赞/收藏做完整往返，核对状态变化
- 安全性：静态扫描 `${}` 拼接、`v-html` 数据来源、CORS 默认值、爬取真实错误日志
- 全程核对后端真实异常日志（不是靠猜）

---

# 七、性能优化（第二轮）

## 7.1 前端：产物从 6.2MB 砍到 1.76MB（省 71%）

| 项目 | 优化前 | 优化后 | 说明 |
|------|--------|--------|------|
| **首页 hero 图** | 2,741 KB (PNG) | **277 KB** (WebP) | 省 90% |
| **默认封面图** | 1,587 KB (PNG) | **129 KB** (WebP) | 省 92% |
| **主 JS 包** | 1,108 KB | **251 KB** | Element Plus 按需引入 + 移除无用图标 |
| **主 CSS 包** | 376 KB | 按页面拆分，最大 38 KB | 组件样式跟着组件走 |
| **@element-plus/icons-vue** | 202 KB | **0** | 全量注册了 ~300 个图标，实测一个都没用到 |
| **产物合计** | ~6.2 MB | **1.76 MB** | **省 71%** |

**三个具体动作：**

1. **图片转 WebP**（收益最大）
   两张装饰图原本是 4.3MB 的 PNG。装饰插画用 PNG 是最差的选择，转成 WebP（质量 82）后只要 406KB，肉眼几乎看不出差别。

2. **Element Plus 改按需引入**
   项目只用了 **15 个**组件，却全量引入了 1MB JS + 376KB CSS。
   改用 `unplugin-vue-components` + `ElementPlusResolver` 后，模板里的 `el-*` 自动按需引入、样式也跟着走；
   `ElMessage` / `ElMessageBox` / `v-loading` 这类不走模板的，在 `main.js` 里单独补样式。
   （已逐页截图确认样式没丢：表格、按钮、表单、弹窗全部正常）

3. **删掉全量图标注册**
   `@element-plus/icons-vue` 的 ~300 个图标被全部注册成全局组件，但**全项目一个都没用到**（界面图标全用 emoji）。纯浪费 202KB。

## 7.2 数据库：补齐 9 个索引

用 `EXPLAIN` 实测发现 3 处 **type=ALL（整表扫描）** 和 3 处 **Using filesort（排序落盘）**，已全部补齐：

| 表 | 新索引 | 解决的查询 | 实测结果 |
|----|--------|-----------|---------|
| post | `idx_list (deleted, status, pinned, publish_time)` | 首页文章列表（最高频） | 走索引，**无 filesort** |
| post | `idx_author (author_id, deleted, status)` | 用户主页文章 | 走索引，**无 filesort** |
| post | `idx_series (series_id, deleted, status, publish_time)` | 系列页 | 走索引，**无 filesort** |
| post | `idx_sched (status, scheduled_at)` | 定时发布（**每分钟**跑一次） | `ALL` → `range` |
| post_tag | `idx_tag (tag_id, post_id)` | 标签页按 tag 反查 | 不再全索引扫描 |
| comment | `idx_status_time (status, create_time)` | 侧栏最新评论 | **`ALL` → `ref`** |
| comment | `idx_post_status (post_id, status, create_time)` | 文章评论列表 | 走索引 |
| user | `idx_email (email)` | 找回密码按邮箱查 | **`ALL` → `ref`** |
| subscribe | `idx_token (token)` | 退订按 token 查 | **`ALL` → `ref`** |

**两个关键细节（值得记下来）：**

- **索引列顺序必须照真实 SQL 来定。** 第一版我按「filter + publish_time」建，
  但首页真实排序是 `pinned DESC, publish_time DESC` —— pinned 必须排在前面，否则索引覆盖不了排序，照样 filesort。
  改用真实 SQL 重验后，EXPLAIN 才变成 `Using index`（无 filesort）。**建索引前一定要把真 SQL 捞出来看。**
- **小表上看不到收益，要造数据才能验证。** 12 行数据时 MySQL 嫌索引"不值当"，仍选旧计划；
  临时造 5000 行后，优化器**主动选择**了新索引 —— 这才是上线后的真实行为。

## 7.3 仍需注意 / 未做

| 项 | 说明 |
|----|------|
| **正文搜索用 `LIKE '%关键词%'`** | 无法走索引，数据量大了会变慢。当前文章量小完全够用；将来若上千篇，可以上 MySQL 全文索引或 **Elasticsearch / Meilisearch** |
| **`markdown` 分包 300KB**（含 highlight.js） | 只在文章详情/写文章页加载，不影响首页首屏，可接受 |
| **热门文章 `ORDER BY views DESC`** | 排序无索引可用，需要全表排序。views 是频繁变动的计数列，加索引反而拖慢写入，**故意不加** |
| **深分页** | `LIMIT offset, size` 在超大 offset 时要扫前面的行。博客分页一般不深，暂不处理 |
| **后端 N+1** | 已核查：文章列表的作者/分类/标签都是 JOIN 或批量查询，**没有 N+1** |
| **可选：删冗余索引** | `comment.idx_post` 已被 `idx_post_status` 覆盖（前缀重合），可删。已写在 `newsql.txt` 第 58 条，**默认注释** |

# 个人博客网站 · 上线部署文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0 |
| 编制日期 | 2026 年 09 月 |
| 适用代码版本 | 前后端联调通过版本（含敏感词过滤、访客留痕、回收站） |
| 目标读者 | 部署者（站主本人）/ 运维人员 |
| 文档性质 | **操作手册**（按顺序照做即可），不是设计文档 |

> **与另外两份文档的区别**：
> 《需求文档》回答"要做什么"，《接口文档》回答"前后端怎么对接"，
> 本《部署文档》回答"**怎么把它跑到服务器上**"——全是可直接复制的命令、配置和检查清单。

---

## 目录

1. 部署架构与前置准备
2. 服务器环境搭建
3. 数据库准备
4. 后端部署
5. 前端部署
6. Nginx 配置
7. HTTPS 证书
8. 上线检查清单
9. 常见故障排查
10. 回滚方案
11. 附录：完整配置模板

---

## 1 部署架构与前置准备

### 1.1 部署架构

```
             用户浏览器
                 │  HTTPS (443)
                 ▼
        ┌─────────────────┐
        │      Nginx      │  ← 一台阿里云 ECS
        │  ┌───────────┐  │
        │  │ 前端静态文件 │  │  /            → /var/www/blog/dist
        │  ├───────────┤  │
        │  │  反向代理   │  │  /api/**      → 127.0.0.1:8080
        │  │            │  │  /uploads/**  → 本地磁盘（图片）
        │  ├───────────┤  │
        │  │  sitemap.xml│  │  → 127.0.0.1:8080
        │  └───────────┘  │
        └────────┬────────┘
                 │ 127.0.0.1:8080
                 ▼
        ┌─────────────────┐        ┌──────────────┐
        │ Spring Boot 后端 │───────▶│  MySQL 8.0   │
        │  （java -jar）   │        │  仅监听本机   │
        └─────────────────┘        └──────────────┘
```

**单机部署**：前端、后端、数据库都在同一台 ECS 上，通过 Nginx 对外统一暴露 443。
后端只监听 127.0.0.1，MySQL 只监听 127.0.0.1，**都不对公网开放**，安全边界收敛到 Nginx 一层。

### 1.2 服务器规格建议

| 项 | 建议 | 说明 |
| --- | --- | --- |
| 云厂商 | 阿里云 ECS | 站主要求 |
| 地域 | 华东/华北任选 | 就近访问 |
| 实例 | 2 核 4G，通用型 | 2 核 2G 也能跑，但 JVM + MySQL 会吃紧 |
| 系统盘 | 40G ESSD | 图片多了再挂数据盘 |
| 带宽 | 1~3 Mbps 按量/固定 | 个人博客够用；图片走 CDN 可省带宽 |
| 操作系统 | Alibaba Cloud Linux 3 / Ubuntu 22.04 | 本文以 **Ubuntu 22.04** 为例 |
| 安全组 | 放行 22 / 80 / 443 | **不要**对公网放行 3306 / 8080 |

### 1.3 上线前必须办好的事（**有等待时间，越早越好**）

| 步骤 | 说明 | 预计耗时 |
| --- | --- | --- |
| 1. 买域名 | `.cn` 域名 | 即时 |
| 2. 域名实名认证 | `.cn` 由 CNNIC 强制要求，**不做无法解析** | 1~3 天 |
| 3. ICP 备案 | 阿里云备案控制台提交，需域名已实名 | **7~20 个工作日** |
| 4. （可选）先申请证书 | 用 DNS 验证方式可在备案前拿到证书，见第 7 章 | 10 分钟 |

> ⚠️ **关键点**：阿里云中国大陆节点对**未备案域名会封 80/443**。
> 也就是说备案通过前，域名无法正常对外提供服务，但**可以先做服务器环境和代码部署**，
> 等备案通过后改一下域名解析即可上线。

### 1.4 需要提前准备的信息清单

| 名称 | 示例 | 用途 |
| --- | --- | --- |
| 服务器公网 IP | `47.98.xx.xx` | 登录、解析 |
| 域名 | `codeeras.cn` | 访问入口 |
| 数据库 root 密码 | 强密码 | 建库用 |
| 数据库应用账号密码 | `blog_app` / 强密码 | 后端连接用 |
| JWT 密钥 | 随机 64 位串 | 登录令牌签名 |
| 管理后台账号密码 | `admin` / 强密码 | 内容管理 |
| 备案号 | `浙ICP备xxxxxxxx号` | 页脚展示 |

---

## 2 服务器环境搭建

### 2.0 ★ 先确认你的系统类型（**这一步必须先做**）

阿里云 ECS 的默认镜像有两类，**命令完全不同**：

```bash
cat /etc/os-release
```

看输出里的 `ID` 字段：

| 输出含 | 属于 | 包管理器 | 本章看哪一列 |
|---|---|---|---|
| `ubuntu` / `debian` | Debian 系 | `apt` | **A 列** |
| `alinux` / `centos` / `rocky` / `rhel` / `anolis` | RedHat 系 | `dnf` 或 `yum` | **B 列** |

> ⚠️ **CentOS 7 已停止维护**，官方源已下线，装不了东西。
> 如果你开的是 CentOS 7，建议**重装系统**为 Alibaba Cloud Linux 3 或 Ubuntu 24.04。
>
> 💡 阿里云大部分官方源在国内很快，一般**不需要**额外换镜像源。

---

### 2.1 系统更新与部署用户

```bash
# ---- A 列：Ubuntu / Debian ----
sudo apt update && sudo apt upgrade -y
```

```bash
# ---- B 列：Alibaba Cloud Linux / CentOS / Rocky ----
sudo dnf update -y
```

创建部署用户 —— **应用本身不要用 root 跑**（原因见下方说明）：

```bash
sudo useradd -m -s /bin/bash blog
sudo passwd blog                 # 设个密码，方便以后切换过去看文件
```

> **⚠️ 注意：这里刻意没有把 blog 加入 sudo 组。**
> 理由：如果 blog 有 sudo 权限，那"攻击者拿到应用进程身份"之后就还能提权到 root，
> 最小权限的意义就没了一半。
>
> **分工是这样的：**
> - **搭建环境、改 systemd/Nginx 配置、重启服务** → 用 **root**（或阿里云的 `ecs-user`）
> - **应用进程本身** → 由 systemd 降权到 **blog** 用户跑（见 4.4 节 `User=blog`）
> - **上传 jar/dist、改项目里的配置文件** → 切到 blog 用户操作即可
>
> 觉得切来切去麻烦，也可以给 sudo（`sudo usermod -aG wheel blog`，
> Ubuntu 上写 `sudo` 组），安全性略降但省事，自己权衡。

**这一步先不要 `su - blog`** —— 下面 2.2~2.5 装软件都需要 root 权限，
直接用 root 把环境装完，等到第 4 章部署时才切用户。

（如果你已经按老版本 `su - blog` 进去了，敲 `exit` 回到 root 再继续。）

---

### 2.1.1 为什么不能用 root 跑应用？

不是洁癖，是**出事时的损失量级完全不同**：

| | 用 root 跑 | 用 blog 用户跑 |
|---|---|---|
| 应用被攻破（比如反序列化 RCE）| 攻击者**直接拿到整台服务器**：改系统、装后门、删你所有数据、挖矿 | 攻击者只能动 blog 能碰的东西（你的项目目录）|
| 影响范围 | 全盘 | 受限 |
| 恢复难度 | 基本要重装系统 | 删掉被改的文件即可 |

大部分 Web 漏洞利用成功后的收益，就是"拿到这个进程的身份"。
**你给它什么身份，攻击者就有什么身份** —— 所以给最小的那个。

---

### 2.2 安装 JDK 21

项目用 **Java 21**（Spring Boot 4.x 的硬要求），版本装错**起不来**。

```bash
# ---- A 列：Ubuntu / Debian ----
sudo apt install -y openjdk-21-jdk
```

```bash
# ---- B 列：Alibaba Cloud Linux 3 / Rocky / RHEL ----
# 先确认源里有没有 21（EL8 系的源不一定带，先查一下）
dnf list available "java-21-openjdk*"

# 上面能列出包 → 直接装
sudo dnf install -y java-21-openjdk-devel

# 上面显示 "No matching Packages" → 走下面的「手动安装」方案
```

**验证（两列通用）：**

```bash
java -version
# 期望看到：openjdk version "21.0.x"
```

**⚠️ 如果源里没有 JDK 21**（Alibaba Cloud Linux 3 是 EL8 系，常见），
用官方通用包手动安装 —— 这条路**一定能成**：

> 🔥 **国内服务器务必用国内镜像！**
> Adoptium 官方地址 `api.adoptium.net` 会把你导到境外节点，
> 实测速度只有 **23 KB/s**（194MB 要下 **2 小时 25 分**）✗
> 换成下面华为云的地址，实测 **731 KB/s**（约 4~5 分钟）✓

```bash
# ✅ 国内镜像（华为云，实测可用）
curl -L -o /tmp/jdk21.tar.gz \
  "https://mirrors.huaweicloud.com/openjdk/21.0.2/openjdk-21.0.2_linux-x64_bin.tar.gz"

sudo mkdir -p /opt/jdk21
sudo tar -xzf /tmp/jdk21.tar.gz -C /opt/jdk21 --strip-components=1

# 加到环境变量（对所有环境生效）
sudo tee /etc/profile.d/jdk21.sh > /dev/null <<'EOF'
export JAVA_HOME=/opt/jdk21
export PATH=$JAVA_HOME/bin:$PATH
EOF
source /etc/profile.d/jdk21.sh

# 验证
java -version
# 期望：openjdk version "21.0.2"
```

**备选镜像**（想要更新的补丁版本可以试，路径可能需自行浏览目录）：

| 镜像 | 目录 |
|---|---|
| 中科大 | `https://mirrors.ustc.edu.cn/adoptium/` |
| 清华 TUNA | `https://mirrors.tuna.tsinghua.edu.cn/Adoptium/` |
| 华为云（已验证）| `https://mirrors.huaweicloud.com/openjdk/` |

> 💡 下载还是很慢的话，说明瓶颈在**你 ECS 的公网带宽**（很多入门配置只有 1 Mbps），
> 跟镜像无关了。1 Mbps ≈ 125 KB/s，194MB 大约 26 分钟，忍着下或者临时升带宽。

> ⚠️ 用这种方式装的话，后面 `blog.service` 里的 `ExecStart=/usr/bin/java ...`
> 要改成 `/opt/jdk21/bin/java`。

---

### 2.3 安装 MySQL 8.0

```bash
# ---- A 列：Ubuntu / Debian（24.04 自带的就是 MySQL 8）----
sudo apt install -y mysql-server
sudo systemctl enable --now mysql
```

```bash
# ---- B 列：Alibaba Cloud Linux 3 / CentOS Stream 8+ ----
# ⚠️ 先装这个依赖，否则 MySQL 装不上（ALinux 3 特有的坑）
sudo dnf install -y compat-openssl10

# 然后装 MySQL
sudo dnf install -y mysql-server
sudo systemctl enable --now mysqld
```

> ### ⚠️⚠️ Alibaba Cloud Linux 3 用户注意：上面这条**大概率会失败**
>
> 报错长这样（实测踩过）：
> ```
> Error:
>  Problem: package mysql-server-8.0.46-...al8.x86_64 from alinux3-updates
>           requires mysql(x86-64), but none of the providers can be installed
>    - package mysql-8.0.21-...al8.x86_64 from alinux3-os is filtered out by exclude filtering
> ```
> **原因**：阿里云在自己的 `alinux3-*` 源里**把 `mysql` 基础包 exclude 掉了** ✗
>
> **方案 A（推荐，走阿里云内网源，快）**：
> ```bash
> sudo dnf install -y mysql-server --disableexcludes=all
> sudo systemctl enable --now mysqld
> ```
> `--disableexcludes=all` 会忽略仓库里的排除规则，直接从阿里云源装 ✓
>
> **方案 B（方案 A 不行时用，走 MySQL 官方源）**：
> ```bash
> sudo rpm -Uvh https://repo.mysql.com/mysql84-community-release-el8-1.noarch.rpm
> sudo rpm --import https://repo.mysql.com/RPM-GPG-KEY-mysql-2022
> sudo dnf install -y mysql-community-server --nogpgcheck
> sudo systemctl enable --now mysqld
> ```
> ⚠️ 注意 rpm 里的 **`el8`** 要和你的系统匹配（`platform:al8` → el8 ✓）。
> ⚠️ 这条路走的是 `repo.mysql.com`（境外），包比较大，**可能比较慢**。
>
> **装成功之后注意 root 密码的情况**（两种包不一样，别照抄教程）：
>
> | 装的是哪个 | root 初始密码 | 怎么拿 |
> |---|---|---|
> | MySQL 官方包（方案 B）| **自动生成临时密码** | `sudo grep 'temporary password' /var/log/mysqld.log` |
> | 阿里云包（方案 A，`--disableexcludes`）| **空密码** ⚠️ | 没有日志可查，`mysql_secure_installation` 会提示 `Connecting to MySQL using a blank password` |
>
> ⚠️ 空密码意味着**任何能连上 3306 的人都能以 root 进库** ——
> 所以 `mysql_secure_installation` 这步**不能跳过**，必须马上设一个强密码。

**安全初始化：**

```bash
sudo mysql_secure_installation
```

这个脚本会连问 7 个问题，按下面这样答：

| 提示 | 怎么答 | 为什么 |
|---|---|---|
| `Would you like to setup VALIDATE PASSWORD component?` | **`y`** | 开启密码强度校验 |
| `Please enter 0 = LOW, 1 = MEDIUM and 2 = STRONG:` | **`0`** | ⚠️ 选 0（只要长度 ≥ 8）。选 1/2 会强制"大小写+数字+特殊字符"，等下给应用建账号时会各种被拒，徒增麻烦。库只监听本机，安全性由安全组保证 |
| `New password:` | 输入你的 **root 密码** | **记牢，后面建库要用** |
| `Re-enter new password:` | 再输一遍 | |
| `Do you wish to continue with the password provided?` | `y`（如果出现）| 密码偏弱时的确认 |
| `Remove anonymous users?` | **`y`** | 删匿名用户 |
| `Disallow root login remotely?` | **`y`** | 禁止 root 远程登录 |
| `Remove test database and access to it?` | **`y`** | 删测试库 |
| `Reload privilege tables now?` | **`y`** | 重载权限表 |

> 💡 建议 root 密码**不要和应用账号密码相同**。root 只有你自己建库时用，
> 应用连库用的是低权限账号 `blog_app`（见 3.2 节）。

> ### ⚠️ 常见卡点：卡在 `New password:` 反复重试，永远过不去
>
> 现象（实测踩过）：
> ```
> New password:
> Re-enter new password:
> Estimated strength of the password: 50
> Do you wish to continue with the password provided? : y
>  ... Failed! Error: Your password does not satisfy the current policy requirements
> New password:            ← 又回到开头，无限循环
> ```
>
> **原因**：你上一步选的密码策略是 MEDIUM/STRONG（不是 0），
> 而你输入的密码不满足"大小写 + 数字 + 特殊字符"的要求 ✗
>
> **两条出路，选一条：**
>
> **出路 1：直接输一个够强的密码**（不用退出脚本）
> 要求：**≥ 8 位，且同时含大写、小写、数字、特殊字符**。
> 例如这种格式（⚠️ 自己改几个字符，别原样用）：
> ```
> Mq7@Kp2Zx9v
> ```
> 💡 特殊字符建议用 `@` `.` `_` `-`，**避开 `#` `!` `$`** 这类在 shell 里有特殊含义的字符
> （虽然这里是输给 MySQL 的，但以后万一要在命令行里用同一个密码会麻烦）。
>
> **出路 2：按 `Ctrl+C` 退出，把策略降到 LOW 再重来**（推荐给不想记复杂密码的）
>
> ```bash
> # 1) 先按 Ctrl+C 退出当前脚本
>
> # 2) 用空密码进 MySQL（此时还没设密码）
> sudo mysql -uroot
>
> # 3) 把策略降成 LOW（只要长度 ≥ 8）
> SET GLOBAL validate_password.policy = LOW;
> SET GLOBAL validate_password.length = 8;
> exit
>
> # 4) 重新跑初始化脚本，这次密码就好设了
> sudo mysql_secure_installation
> ```
>
> ⚠️ **注意**：`SET GLOBAL` **重启 MySQL 后会失效**。想永久生效要写进配置文件：
> ```bash
> sudo tee -a /etc/my.cnf.d/validate-password.cnf <<'EOF'
> [mysqld]
> validate_password.policy = LOW
> validate_password.length = 8
> EOF
> sudo systemctl restart mysqld
> ```
>
> 💡 **为什么建议降成 LOW**：这个策略对**所有** MySQL 密码生效，
> 包括等下要建的 `blog_app` 应用账号。策略太严的话，建账号时会再次被拒 ✗
> 库只监听本机（外部连不上），安全性由阿里云安全组保证，LOW 足够。

**确认 MySQL 只监听本机**（很重要，防止公网直连数据库）：

```bash
# A 列
sudo grep -r "bind-address" /etc/mysql/

# B 列
sudo grep -i "bind-address" /etc/my.cnf /etc/my.cnf.d/*.cnf 2>/dev/null
```

应该是 `bind-address = 127.0.0.1`。若被改过，改回来再 `sudo systemctl restart mysql`（B 列是 `mysqld`）。

**验证：**

```bash
mysql -uroot -p -e "SELECT VERSION();"
# 期望看到 8.0.x 或 8.4.x
```

---

### 2.4 安装 Nginx

```bash
# ---- A 列 ----
sudo apt install -y nginx
sudo systemctl enable --now nginx
```

```bash
# ---- B 列 ----
sudo dnf install -y nginx
sudo systemctl enable --now nginx
```

```bash
nginx -v          # 确认装上
```

---

### 2.5 安装 Node.js（**可选，你本地已构建好前端，可以跳过**）

前端产物 `dist/` 我在本地已经构建好了，服务器**不需要装 Node**，
直接把 `dist/` 上传即可。只有将来想在服务器上改前端代码时才需要：

```bash
# ---- A 列 ----
curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
sudo apt install -y nodejs

# ---- B 列 ----
curl -fsSL https://rpm.nodesource.com/setup_22.x | sudo bash -
sudo dnf install -y nodejs
```

---

### 2.6 ★ 阿里云安全组放行端口（**必做，否则外网打不开**）

去阿里云控制台 → ECS 实例 → **安全组** → 配置规则 → 入方向，添加：

| 端口 | 用途 | 授权对象 |
|---|---|---|
| **80** | HTTP（申请证书要验证这个）| `0.0.0.0/0` |
| **443** | HTTPS（上线后用户访问）| `0.0.0.0/0` |
| 22 | SSH 登录 | 建议只填**你自己的 IP** |

> ⚠️ **绝对不要**放行 3306（MySQL）和 8080（后端）—— 它们只监听本机，
> 由 Nginx 反代访问，放出去等于把数据库和接口直接暴露到公网。

如果服务器装了防火墙（`firewalld` / `ufw`）也要对应放行：

```bash
# B 列（firewalld）
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload

# A 列（ufw，若启用）
sudo ufw allow 80/tcp && sudo ufw allow 443/tcp && sudo ufw allow 22/tcp
```

---

## 3 数据库准备

### 3.1 建库 + 导入表结构与初始数据

把项目的 `sql/sql.txt` 上传到服务器（如 `/home/blog/`），然后执行：

```bash
mysql -uroot -p < /home/blog/sql.txt
```

该脚本会创建 `blog` 库、全部数据表，以及 12 篇示例文章（使用 `INSERT IGNORE`，**可重复执行**）。

> 已有旧库需要升级结构时，改为执行 `newsql.txt`（增量脚本，幂等）。

### 3.2 创建低权限应用账号（**重要**）

**不要让后端直接用 root 连数据库**。创建只有增删改查权限的账号：

```bash
mysql -uroot -p
```

```sql
CREATE USER 'blog_app'@'localhost' IDENTIFIED BY '换成你的强密码';
GRANT SELECT, INSERT, UPDATE, DELETE ON blog.* TO 'blog_app'@'localhost';
FLUSH PRIVILEGES;
```

验证：

```bash
mysql -ublog_app -p -e "SELECT COUNT(*) FROM blog.post;"
```

> 该账号**没有建表/改表权限**。以后需要改结构时，用 root 手动执行 `newsql.txt`。
> 后端启动时如果发现表缺失，会在日志里提示，但不会报错中断。

### 3.3 创建管理员账号（**必须手动执行**）

程序**不会**自动创建管理员（避免默认密码被猜到导致站点被接管）：

```bash
mysql -uroot -p < /home/blog/tools/create-admin.sql
```

该脚本幂等，创建 `admin / admin123`。**登录后请立刻在「个人信息」页改密码**。

想直接设成自己的密码，先用工具生成哈希：

```bash
java -cp "spring-security-crypto-6.5.0.jar:commons-logging-1.2.jar" tools/GenHash.java 你的新密码
```

把输出的 HASH 填进 `tools/create-admin.sql` 再执行。

---

## 4 后端部署

### 4.1 打包

**在本地**（或在服务器上装了 Maven 后）打包：

```bash
cd back-ed/backed-demo
mvn clean package -DskipTests
# 产物：target/backed-demo-<版本>.jar（可执行 fat jar）
```

### 4.2 上传与目录规划

```bash
# 服务器上建立目录
sudo mkdir -p /opt/blog/{app,uploads,logs}
sudo chown -R blog:blog /opt/blog
```

用 `scp` 或宝塔/FTP 上传：

```bash
scp target/backed-demo-*.jar blog@<服务器IP>:/opt/blog/app/blog.jar
```

最终目录结构：

```
/opt/blog/
├── app/
│   ├── blog.jar                 # 后端可执行 jar
│   └── application-local.yml    # 私有配置（数据库密码 / JWT 密钥）
├── uploads/                     # 用户上传的图片
└── logs/
    └── blog.log
```

### 4.3 生产配置

新建 `/opt/blog/app/application-local.yml`（**不要提交到版本库**）：

```yaml
spring:
  datasource:
    username: blog_app
    password: 你的数据库密码

blog:
  jwt:
    secret: 你的随机密钥
```

生成随机密钥：

```bash
node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
# 没有 node 时用 openssl
openssl rand -base64 48
```

**更推荐用环境变量注入**（配置与密钥分离，systemd 里写）：

| 环境变量 | 说明 |
| --- | --- |
| `BLOG_JWT_SECRET` | ⚠️ 必须设置，否则用占位符（启动会打印告警） |
| `DB_USERNAME` / `DB_PASSWORD` | 数据库账号密码 |
| `BLOG_TRUST_PROXY` | 在 Nginx 后必须为 `true`，否则拿到的都是代理 IP |
| `BLOG_CORS_ORIGINS` | 同源部署时可不设 |
| `BLOG_RATE_LIMIT` | 是否开启限流，默认 `true` |

### 4.4 配置 systemd 开机自启

新建 `/etc/systemd/system/blog.service`：

```ini
[Unit]
Description=Personal Blog Backend
After=network.target mysql.service
Wants=mysql.service

[Service]
Type=simple
User=blog
WorkingDirectory=/opt/blog/app
# 把 uploads / logs 指到上面规划好的绝对路径

# ⚠️ 环境变量优先级 高于 application-local.yml：
#    用这个文件配敏感值，就要把下面同名的 Environment= 行删掉/注释掉
Environment="BLOG_JWT_SECRET=你的随机密钥"
Environment="DB_USERNAME=blog_app"
Environment="DB_PASSWORD=你的数据库密码"
Environment="BLOG_TRUST_PROXY=true"

# ★ 邮件通知：不配则「找回密码」失效、订阅推送/评论通知/500 告警全部发不出去
Environment="MAIL_HOST=smtp.qq.com"
Environment="MAIL_PORT=465"
Environment="MAIL_USERNAME=你的QQ邮箱@qq.com"
Environment="MAIL_PASSWORD=你的16位SMTP授权码"
Environment="BLOG_MAIL_FROM=你的QQ邮箱@qq.com"
Environment="BLOG_ALERT_EMAIL=收告警的邮箱@qq.com"

# ★ 备份目录：必须和 deploy.sh 的 BACKUP_DIR 一致
Environment="BLOG_BACKUP_DIR=/opt/blog/backup"
Environment="BLOG_BACKUP_KEEP=7"
Environment="BLOG_MYSQLDUMP=mysqldump"

ExecStart=/usr/bin/java -Xms256m -Xmx512m -jar /opt/blog/app/blog.jar \
  --server.port=8080 \
  --blog.upload.dir=/opt/blog/uploads/ \
  --logging.file.name=/opt/blog/logs/blog.log
# 崩溃自动拉起
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
```

> `Restart=on-failure` + `RestartSec=5` 保证进程异常退出后 5 秒自动重启。
> 正常停止（`systemctl stop`）不会被重启，放心使用。

> ⚠️ **重要：打包出来的 jar 里带着你本地的 `application-local.yml`**
> （因为它在 `src/main/resources/` 下，`mvn package` 会一起打进去）。
> 这带来两个后果，都要记住：
>
> 1. **好处**：上传 jar 就自动带上了邮件配置，不用在服务器上重复填
> 2. **⚠️ 风险**：这个 jar 里含你的**数据库密码、SMTP 授权码、JWT 密钥** ——
>    **千万不要把它发到 GitHub Releases、网盘或发给别人**
> 3. **⚠️ 必做**：服务器上**一定要用环境变量覆盖**数据库密码和 JWT 密钥
>    （jar 里带的是你**本地**的值，直接用等于把本地密码搬到了线上）
>    —— `blog.service` 里已经配好这两个变量，照着填即可
>
> 想彻底避免（进阶做法）：把 `application-local.yml` 移到项目根目录，
> 在 `pom.xml` 里用 `maven-resources-plugin` 排除它，只靠外部文件/环境变量配置。
> 注意这样会让 IDEA 本地运行也读不到该文件，需要一并调整。

**权限是怎么落地的？（为什么 `systemctl` 用 root 敲，进程却是 blog 跑的）**

`blog.service` 里的 `User=blog` / `Group=blog` 是关键：

```
你用 root 执行 systemctl start blog
        ↓
systemd（root 身份）读取 blog.service
        ↓
fork 出子进程后，把身份降权成 blog:blog
        ↓
真正跑起来的 java 进程 = blog 用户  ✓
```

所以 —— **启动命令用 root 敲是正常的**，但**应用进程本身不是 root** ✓

**两条命令验证（部署完必查）：**

```bash
systemctl status blog
# 输出里应看到：  CGroup: /system.slice/blog.service ... java -jar /opt/blog/app/blog.jar

ps -o user,pid,cmd -C java
# USER 列必须是 blog，不是 root
```

**⚠️ 配套的目录属主（很容易漏，漏了会出怪问题）**

应用要往这几个目录**写**东西，所以它们必须属于 blog：

| 目录 | 应用往里写什么 | 属主不对会怎样 |
|---|---|---|
| `/opt/blog/uploads` | 用户上传的图片 | 后台传图报 500 |
| `/opt/blog/logs` | 运行日志 | 日志写不进去 |
| `/opt/blog/backup` | 数据库备份 | 备份失败 |
| `/var/www/blog` | （Nginx 读，只需可读）| 网站打不开 |

`deploy.sh` 里已经有一句 `chown -R blog:blog /opt/blog /var/www/blog` ✓
如果你手工部署，**这条一定要执行**：

```bash
sudo chown -R blog:blog /opt/blog /var/www/blog
sudo chmod 750 /opt/blog/uploads /opt/blog/logs
```

> 🔍 **出问题时的排查线索**：如果功能报 500 但日志里没什么信息，
> 先看 `ls -l /opt/blog/uploads` 的属主对不对 —— 这是最常见的坑。

启动并设为开机自启：

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now blog
sudo systemctl status blog
```

查看日志：

```bash
sudo journalctl -u blog -f          # systemd 捕获的启动日志
tail -f /opt/blog/logs/blog.log     # 应用自身的日志文件
```

验证后端（此时还没配 Nginx，直连本机端口）：

```bash
curl http://127.0.0.1:8080/api/site-info
# 期望返回 {"code":200,"data":{...}}
```

启动日志里应看到：

- `敏感词库加载完成，共 22 条`
- 如果出现 `⚠️ 安全告警：JWT 密钥还是占位符！` → 说明环境变量没生效，必须排查

---

### 4.5 邮件与告警配置（★ 不配会丢功能，务必检查）

很多人部署完「看起来一切正常」，但邮件相关的功能**静默失效** —— 因为程序设计成「没配邮件就跳过发送，不报错」，只是什么都不发。

**不配的后果：**

| 功能 | 表现 |
|---|---|
| **找回密码** | ❌ **直接失效**。用户忘记密码后**没有任何办法找回**，只能你手动改库 |
| 新文章推送订阅者 | 静默失败，订阅者永远收不到 |
| 评论回复通知 | 静默失败 |
| 站点 500 错误告警 | 收不到，出问题你不知道 |

> ⚠️ 本地开发时这些是好的（配置写在 `application-local.yml` 里），但那个文件**不会提交到 Git**，所以服务器上是空的。

**配置方式**（与 4.4 的两种方式一致，选一种，不要混用）

**方式 A：systemd 环境变量**——即在 `blog.service` 里填这几行：

```ini
Environment="MAIL_HOST=smtp.qq.com"
Environment="MAIL_PORT=465"
Environment="MAIL_USERNAME=你的QQ邮箱@qq.com"
Environment="MAIL_PASSWORD=你的16位SMTP授权码"
Environment="BLOG_MAIL_FROM=你的QQ邮箱@qq.com"
Environment="BLOG_ALERT_EMAIL=收告警的邮箱@qq.com"
```

**方式 B：`application-local.yml`**——复制 `deploy/application-local.yml.example` 到 `/opt/blog/app/application-local.yml`，填 `spring.mail.*` 与 `blog.mail.from`、`blog.alert.email`。

**拿到 SMTP 授权码**（以 QQ 邮箱为例）：

1. 登录 QQ 邮箱 → 设置 → 账户
2. 找到「POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务」
3. 开启 **POP3/SMTP 服务**，按提示验证后**生成授权码**
4. 那串 **16 位字母**就是 `MAIL_PASSWORD`（**不是 QQ 登录密码**）

> `BLOG_MAIL_FROM` 必须和 `MAIL_USERNAME` **完全一致**，这是 QQ 邮箱的规则，不一致会报错。

**验证配置是否生效**（两种方式都适用）：

```bash
# 1) 重启后看日志有没有邮件相关报错
journalctl -u blog -n 50 | grep -i mail

# 2) 最直接：登录后台 →「站点设置」→ 邮件通知处点「发送测试邮件」
#    真发一封到你邮箱，收到就说明通了
```

---

> 💡 **另一种更省事的做法**：直接把你**本地开发用的** `application-local.yml` 上传到 `/opt/blog/app/`，只改两处：
> 1. `spring.datasource.password` 换成服务器的数据库密码
> 2. `blog.backup.mysqldump` 那一行删掉（Linux 走 PATH 即可）
>
> 这样邮件、JWT 密钥全都一次带过去了。⚠️ 但记得 `chmod 600` 并且属主给 `blog`。

---

## 5 前端部署

### 5.1 构建

**在本地**构建（服务器不用装 Node）：

```bash
cd front/front-demo
npm install
npm run build
# 产物目录：dist/
```

### 5.2 上传

```bash
sudo mkdir -p /var/www/blog
sudo chown -R blog:blog /var/www/blog
scp -r dist/* blog@<服务器IP>:/var/www/blog/
```

> 前端请求走的是相对路径 `/api`，同源部署**不需要改任何配置**。

---

## 6 Nginx 配置

### 6.1 站点配置

新建 `/etc/nginx/sites-available/blog`：

```nginx
server {
    listen 80;
    server_name codeeras.cn www.codeeras.cn;

    # 备案通过前可先只配 80；证书就绪后由第 7 章改成跳转 443
    root /var/www/blog;
    index index.html;

    # 上传大小（后端本身限制 5MB，这里放宽一点避免误拦）
    client_max_body_size 10m;

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;
    gzip_min_length 1024;

    # ① 前端静态资源（带 hash 的文件长期缓存）
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # ② 用户上传的图片：直接由 Nginx 读磁盘，不经过后端
    location /uploads/ {
        alias /opt/blog/uploads/;
        expires 7d;
        add_header Cache-Control "public";
    }

    # ③ 后端接口
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        # ★ 必须透传真实 IP，否则限流和访客留痕会把所有人当成同一个 IP
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }

    # ④ 站点地图（后端生成，路径不带 /api 前缀）
    location = /sitemap.xml {
        proxy_pass http://127.0.0.1:8080/sitemap.xml;
        proxy_set_header Host            $host;
        proxy_set_header X-Real-IP       $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        add_header Content-Type application/xml;
    }

    # ⑤ SPA 前端路由回退：刷新 /posts/3 这类地址时交给 index.html
    #    必须放在最后，且不能影响上面几条
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

启用配置：

```bash
sudo ln -s /etc/nginx/sites-available/blog /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default      # 移除默认站点
sudo nginx -t                                     # 语法检查
sudo systemctl reload nginx
```

### 6.2 关于错误状态码透传（易踩坑）

本项目已把 HTTP 状态码规范化（401 / 403 / 404 / 429 都返回对应状态码）。
Nginx 默认 `proxy_intercept_errors off`，上游状态码会**原样透传**，无需额外配置。

**但**如果你在别处配置了 `proxy_intercept_errors on;` 或 `error_page`，
再叠加自定义错误页，可能会把 401/429 **吞成 200 或统一错误页**，
导致前端拦截器拿不到 401 而无法正确跳登录页。**保持默认即可。**

---

## 7 HTTPS 证书

### 7.1 两种验证方式的区别（**备案前后都能办**）

| 方式 | 前提 | 何时可用 |
| --- | --- | --- |
| HTTP-01 | 域名的 80 端口能被公网访问 | **必须备案通过后** |
| **DNS-01** | 在域名 DNS 加一条 TXT 记录 | **备案前就能申请** |

结论：**想早点拿证书，用 DNS-01**；否则等备案通过后用 HTTP-01 更简单。

### 7.2 用 HTTP-01（备案通过后，最省事）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d codeeras.cn -d www.codeeras.cn
```

certbot 会自动改好 Nginx 配置并加上续期定时任务。验证自动续期：

```bash
sudo certbot renew --dry-run
```

### 7.3 用 DNS-01（备案前也能拿到）

以阿里云 DNS 为例：

```bash
sudo apt install -y certbot python3-certbot-dns-aliyun
# 申请后手动在阿里云 DNS 控制台添加提示的 TXT 记录
sudo certbot certonly --manual --preferred-challenges dns -d codeeras.cn
```

证书生成在 `/etc/letsencrypt/live/codeeras.cn/`。

### 7.4 启用 443 与强制跳转

证书就绪后，把 6.1 的 server 块改成：

```nginx
# 80 端口全部跳转到 HTTPS
server {
    listen 80;
    server_name codeeras.cn www.codeeras.cn;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name codeeras.cn www.codeeras.cn;

    ssl_certificate     /etc/letsencrypt/live/codeeras.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/codeeras.cn/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    ssl_session_cache   shared:SSL:10m;
    ssl_session_timeout 10m;

    add_header Strict-Transport-Security "max-age=31536000" always;

    # ……此处粘贴 6.1 中 root / location 那几段（内容完全一致）
}
```

```bash
sudo nginx -t && sudo systemctl reload nginx
```

### 7.5 证书到期提醒

Let's Encrypt 证书有效期 90 天，certbot 会自动续期。
建议额外加一条监控（如阿里云云监控的站点探测），到期前 15 天告警。

---

## 8 上线检查清单

按顺序打勾，**任意一项不通过就不要上线**：

### 8.1 安全项

- [ ] 管理后台密码已从 `admin123` 改成强密码
- [ ] `BLOG_JWT_SECRET` 已设为随机长串，启动日志**没有**占位符告警
- [ ] 后端使用 `blog_app` 低权限账号连库，**不是 root**
- [ ] MySQL 只监听 `127.0.0.1`，公网无法直连 3306
- [ ] 后端 8080 只监听本机，安全组未对公网放行
- [ ] 服务器 SSH 已禁用 root 直接登录或改非默认端口
- [ ] HTTPS 已启用，HTTP 自动跳转 443
- [ ] `.gitignore` 中的 `application-local.yml` 未被提交（如使用 Git）
- [ ] 测试残留数据已清理（测试文章、测试用户）

### 8.2 功能项

- [ ] 首页文章列表能正常加载（12 篇示例文章）
- [ ] 文章详情页正文、目录导航正常
- [ ] 登录 / 注册正常，登录前能看到免责声明弹窗
- [ ] 后台能进入，各菜单（文章 / 回收站 / 分类 / 标签 / 评论 / 敏感词 / 友链 / 媒体库 / 订阅 / 访客记录 / 设置 / 统计）均可打开
- [ ] 发表带敏感词的评论被正确拦截
- [ ] 访客留痕：浏览几个页面后，后台「访客记录」能看到记录、统计页趋势图有柱子
- [ ] 图片上传后能正常显示（`/uploads/` 路径可访问）
- [ ] `/sitemap.xml` 和 `/api/rss` 能正常打开
- [ ] **★ 邮件链路**：后台「站点设置」→ 邮件通知 → 点「发送测试邮件」，**能真收到**
- [ ] **★ 找回密码**：用真实邮箱走一遍「忘记密码」，能收到重置邮件并成功改密
- [ ] **★ 邮件订阅**：前台用真实邮箱订阅，发一篇新文章，订阅者能收到通知

### 8.3 配置项

- [ ] **★ 邮件配置已填**（`MAIL_HOST` / `MAIL_USERNAME` / `MAIL_PASSWORD` / `BLOG_MAIL_FROM`）
      —— 不填的话上面三项功能会静默失效，详见 4.5 节
- [ ] `BLOG_ALERT_EMAIL` 已填，且**故意制造一次 500 错误**确认能收到告警
- [ ] **ICP 备案号已填入站点设置**，页脚显示且可点击跳转工信部
- [ ] **公安联网备案号已填**（拿到后填，页脚会自动生成可点击链接）
- [ ] 站点名称与 **ICP 备案时申报的网站名称一致**（公安备案会核对）
- [ ] 站点名称、副标题、Logo、描述已按需修改（不再是「我的博客」这类默认值）
- [ ] 冗余的示例文章、测试账号已清理或保留确认（清理语句见 `newsql.txt` 第十一批）
- [ ] `BLOG_BACKUP_DIR` 指向 `/opt/blog/backup`，且后台「立即备份」能生成文件
- [ ] 隐私政策 / 免责声明页面内容已按实际情况调整

---

## 9 常见故障排查

| 现象 | 可能原因 | 处理 |
| --- | --- | --- |
| 页面能开，接口全部 502 | 后端没起来 | `systemctl status blog`、`journalctl -u blog -n 100` |
| 启动日志提示 JWT 占位符告警 | 环境变量没生效 | 检查 systemd 的 `Environment=` 与 `ExecStart` |
| 登录报"用户名或密码错误"但密码没错 | 管理员没建 / 密码被改过 | 执行 `tools/create-admin.sql`，或按 `newsql.txt` 第五节重置 |
| 后端启动报 `Access denied for user 'blog_app'` | 授权没做 / 密码不符 | 重跑第 3.2 节授权语句 |
| 图片上传成功但显示 404 | Nginx 的 `/uploads/` 路径没配或路径不一致 | 核对 `alias` 与 `--blog.upload.dir` 是否指向同一目录 |
| 刷新 `/posts/3` 返回 404 | 缺 SPA 回退 | 确认 `location / { try_files $uri $uri/ /index.html; }` 存在 |
| 访客记录里 IP 全是 `127.0.0.1` | 未透传真实 IP | Nginx 加 `X-Forwarded-For`，后端 `BLOG_TRUST_PROXY=true` |
| 登录提示"操作太频繁" | 触发了限流（10 次 / 5 分钟） | 等 5 分钟，或调大 `RateLimitInterceptor` 里的阈值 |
| 前端报 401 但不跳登录页 | Nginx 拦掉了 401 状态码 | 关闭 `proxy_intercept_errors`（见 6.2） |
| 样式错乱 / 页面空白 | 前端构建产物没上传完整 | 重新上传 `dist/` 全部内容 |

---

## 10 回滚方案

### 10.1 代码回滚

```bash
# 上传新版本前先备份旧 jar
cp /opt/blog/app/blog.jar /opt/blog/app/blog.jar.bak.$(date +%Y%m%d%H%M)

# 回滚
cp /opt/blog/app/blog.jar.bak.20260912 /opt/blog/app/blog.jar
sudo systemctl restart blog
```

前端同理：保留上一版 `dist` 目录，直接换回去。

### 10.2 数据回滚

**每次改结构或批量改数据前，先备份数据库**：

```bash
mysqldump -uroot -p --single-transaction --default-character-set=utf8mb4 blog \
  > /opt/blog/backup/blog_$(date +%Y%m%d).sql
```

恢复：

```bash
mysql -uroot -p blog < /opt/blog/backup/blog_20260912.sql
```

建议加一条 crontab 每天自动备份：

```bash
0 3 * * * mysqldump -uroot -p'密码' --single-transaction blog > /opt/blog/backup/blog_$(date +\%Y\%m\%d).sql
```

---

## 11 附录：完整配置模板

### 11.1 目录速查

```
/opt/blog/
├── app/
│   ├── blog.jar
│   └── application-local.yml
├── uploads/          ← Nginx alias 指向这里
├── logs/blog.log
└── backup/
/var/www/blog/        ← 前端 dist 内容
/etc/systemd/system/blog.service
/etc/nginx/sites-available/blog
```

### 11.2 常用运维命令

```bash
sudo systemctl restart blog        # 重启后端
sudo systemctl status blog         # 查看状态
sudo journalctl -u blog -f         # 实时日志
tail -f /opt/blog/logs/blog.log    # 应用日志
sudo nginx -t && sudo systemctl reload nginx   # 重载 Nginx
```

### 11.3 上线当天执行顺序（速查）

```text
① 备案通过，域名解析到服务器 IP
② certbot 申请证书（HTTP-01）
③ Nginx 切到 443 + 强制跳转
④ 访问 https://codeeras.cn 逐项走第 8 章检查清单
⑤ 登录后台，改 admin 密码、填备案号、调整站点信息
⑥ 删除测试数据，正式发文
```

### 11.4 一键部署脚本（`deploy/` 目录）

项目里已经准备好一套可直接用的部署产物，**不必手抄本文的配置**：

| 文件 | 用途 |
| --- | --- |
| `deploy/deploy.sh` | 一键部署脚本（在服务器上执行） |
| `deploy/nginx-blog.conf` | Nginx 站点配置模板 |
| `deploy/blog.service` | systemd 服务模板 |
| `deploy/application-local.yml.example` | 生产私有配置模板 |

**使用流程**：

```bash
# ① 本地打包（在项目里执行）
cd back-ed/backed-demo && mvn clean package -DskipTests
cd ../../front/front-demo && npm run build

# ② 上传到服务器（把 jar、dist、deploy 一起传上去）
scp back-ed/backed-demo/target/backed-demo-*.jar blog@<服务器IP>:/tmp/blog.jar
scp -r front/front-demo/dist                blog@<服务器IP>:/tmp/dist
scp -r deploy                               blog@<服务器IP>:/tmp/deploy

# ③ 在服务器上执行
ssh blog@<服务器IP>
cd /tmp/deploy && chmod +x deploy.sh
sudo ./deploy.sh --jar /tmp/blog.jar --dist /tmp/dist --domain codeeras.cn
```

脚本会：建目录与运行用户 → 备份旧版本 → 部署 jar 与前端 → 安装 systemd 与 Nginx 配置 →
重启服务 → 做健康检查并打印后续待办。

> **设计上的两个安全点**：
> ① 脚本**不会覆盖已存在的** systemd / Nginx 配置，避免把你填好的 JWT 密钥、数据库密码冲掉；
> ② 部署前会自动备份旧 jar 与前端产物到 `/opt/blog/backup/`，方便回滚。

脚本执行完仍需**手动完成三件事**（属安全设计，脚本不该代劳）：
填 systemd 里的密钥 → 执行 `tools/create-admin.sql` 建管理员 → 申请证书并切 HTTPS。


---

**文档结束**
